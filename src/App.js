import React from "react";

function App() {
  return (
    <div style={{ textAlign: "center", marginTop: "100px" }}>
      <h1>🚀 React App Dockerized via GitHub Actions!</h1>
      <p>This React app was built inside GitHub — no Docker Desktop needed 🎉</p>
    </div>
  );
}

export default App;
